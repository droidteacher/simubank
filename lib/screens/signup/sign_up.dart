import 'package:flutter/material.dart';

class SignUp extends StatelessWidget {
  const SignUp({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Not implmented yet', style: Theme.of(context).textTheme.titleLarge?.copyWith(color: const Color(0xffdc2626)),)
    );
  }
}